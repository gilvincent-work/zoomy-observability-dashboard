import {SettingsTab} from '@/components/analyst/tabs';

export const dynamic = 'force-dynamic';

// Shell for account (single-owner auth) + digest preferences — backlog features.
export default function Page() {
  return <SettingsTab />;
}
