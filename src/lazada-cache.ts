/** Cache tag + window for the Lazada readers. Uploads revalidate the tag, so a
 * fresh import shows immediately rather than waiting the window out. */
export const LAZADA_TAG = 'lazada-orders';
export const LAZADA_CACHE_REVALIDATE = 60;
